---
apiVersion: v1
kind: Service
metadata:
  name: imni-tools
  namespace: tools
  labels:
    app: imni-tools
spec:
  selector:
    app: imni-tools
  type: ClusterIP
  ports:
    - port: 80
      name: web
      targetPort: 80
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: imni-tools
  namespace: tools
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "5000m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
  labels:
    app: imni-tools
spec:
  ingressClassName: nginx
  rules:
  - host: imni-tools.github.icu
    http:
      paths:
      - backend:
          service:
            name: imni-tools
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - imni-tools.github.icu
    secretName: github-tls
