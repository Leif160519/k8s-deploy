apiVersion: v1
kind: Service
metadata:
  labels:
    app: vmware-exporter
  name: vmware-exporter
  namespace: monitoring
spec:
  ports:
  - name: http
    port: 9169
    protocol: TCP
    targetPort: 9169
  selector:
    app: vmware-exporter
  type: ClusterIP
