apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: windows-data
  namespace: test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 70Gi
  storageClassName: nfs-storage
